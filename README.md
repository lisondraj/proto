# Proto

Standalone Next.js site for Proto — home (`/`) and investors (`/about`).

## Routes

| Route | Description |
|-------|-------------|
| `/` | Proto landing (phone + desktop) |
| `/about` | For Investors article |

## Develop

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
