/** Proto iPhone — one gutter + clearance system for home and /about. */

/** Apply horizontal page gutters (pairs with proto.css custom properties). */
export const PROTO_PHONE_PAGE_INSET_X = "proto-phone-inset-x";

/** Main content below fixed nav — home feature bands and /about article. */
export const PROTO_PHONE_CONTENT_PT = "proto-phone-content-pt";

export const PROTO_MORE_ABOUT_PHONE_TRACK_START_SPACER =
  "proto-more-about__track-spacer proto-more-about__track-spacer--start shrink-0";

export const PROTO_MORE_ABOUT_PHONE_TRACK_END_SPACER =
  "proto-more-about__track-spacer proto-more-about__track-spacer--end shrink-0";

/** Title row — same left edge as cards. */
export const PROTO_MORE_ABOUT_PHONE_TITLE_INSET = PROTO_PHONE_PAGE_INSET_X;
