/** /proto hero — one full stable viewport band (matches feature sections). */
export const PROTO_HERO_HEIGHT = "var(--app-vh,100lvh)";

/** Space between hero and the first feature shader box. */
export const PROTO_HERO_TO_FEATURES_PAD_TW =
  "pt-[clamp(2.75rem,7.5vh,5.5rem)]";
