/** Proto site chrome — nav fill is the primary page background. */
export const PROTO_NAV_BG = "#121819";

/** Main page + /about canvas (matches nav bar fill). */
export const PROTO_PAGE_BG = PROTO_NAV_BG;

/** Slightly lighter band for “More about Proto” and elevated surfaces. */
export const PROTO_PAGE_BG_LIGHT = "#151c1f";

export const PROTO_BORDER = "#2A3538";
