import { outfit, plusJakartaSans } from "@/lib/home/fonts";

/** /proto page typography — Plus Jakarta Sans. */
export const PROTO_FONT = plusJakartaSans;
export const PROTO_FONT_CLASS = plusJakartaSans.className;
export const PROTO_FONT_VARIABLE = plusJakartaSans.variable;

/** /proto nav wordmark only — Outfit (page body stays Plus Jakarta Sans). */
export const PROTO_NAV_LOGO_FONT = outfit;
export const PROTO_NAV_LOGO_FONT_CLASS = `${outfit.className} proto-nav-logo`;
export const PROTO_NAV_LOGO_FONT_VARIABLE = outfit.variable;
