import { DOEPHONE_COMMUNICATION_SLIDES } from "@/lib/doephone/communication-carousel";

/** Same radial fill as the join hero / Agents carousel card — gradient only, no grid. */
export const JOIN_AGENTS_GRADIENT = DOEPHONE_COMMUNICATION_SLIDES[0].backdrop.gradient;
