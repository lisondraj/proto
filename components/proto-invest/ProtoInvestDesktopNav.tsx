"use client";

import { ProtoDesktopFrostNav } from "@/components/proto/ProtoDesktopFrostNav";

/** Desktop /about nav — proto dark chrome with punched-out frost pill on scroll. */
export function ProtoInvestDesktopNav() {
  return <ProtoDesktopFrostNav ariaLabel="Site" />;
}
