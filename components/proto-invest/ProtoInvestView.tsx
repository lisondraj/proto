"use client";

import { ProtoInvestRouter } from "@/components/proto-invest/ProtoInvestRouter";

/** /about — investor mission page with responsive proto chrome. */
export function ProtoInvestView() {
  return <ProtoInvestRouter />;
}
